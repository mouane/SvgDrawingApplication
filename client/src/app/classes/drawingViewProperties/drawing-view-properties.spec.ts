import { DrawingViewProperties } from './drawing-view-properties';

describe('DrawingViewProperties', () => {
  it('should create an instance', () => {
    expect(new DrawingViewProperties()).toBeTruthy();
  });
});
