import { OpenDrawingProperties } from './open-drawing-properties';

describe('OpenDrawingProperties', () => {
  it('should create an instance', () => {
    expect(new OpenDrawingProperties()).toBeTruthy();
  });
});
