import { SaveDrawingProperties } from './save-drawing-properties';

describe('SaveDrawingProperties', () => {
  it('should create an instance', () => {
    expect(new SaveDrawingProperties()).toBeTruthy();
  });
});
