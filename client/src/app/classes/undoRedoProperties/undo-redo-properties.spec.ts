import { UndoRedoProperties } from './undo-redo-properties';

describe('UndoRedoProperties', () => {
  it('should create an instance', () => {
    expect(new UndoRedoProperties()).toBeTruthy();
  });
});
