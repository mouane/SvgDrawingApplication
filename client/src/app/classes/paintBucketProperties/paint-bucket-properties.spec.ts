import { PaintBucketProperties } from './paint-bucket-properties';

describe('PaintBucketProperties', () => {
  it('should create an instance', () => {
    expect(new PaintBucketProperties()).toBeTruthy();
  });
});
